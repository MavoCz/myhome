{
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "generate-api": "cd backend && ./gradlew generateOpenApiDocs && cd .. && orval"
  },
  "devDependencies": {
    "orval": "^7.8.0"
  }
}
